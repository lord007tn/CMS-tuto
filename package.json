{
  "name": "cms-tuto",
  "version": "0.1.0",
  "description": "CMS example project",
  "main": "index.js",
  "scripts": {
    "client": "cd client && npm start ",
    "server": "cd server && npm start ",
    "dev": "concurrently -n 'server,client' -c 'red,green' \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "CMS"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
